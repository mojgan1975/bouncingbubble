var on = false;



function setup(){
    createCanvas(600,400);
}

function draw(){
    if (on) {
        background(0,255,0);
    }
background(0);

    background(0,255,0);
}
stroke(255);
strokeWeight(4);
noFill();
if (mouseX > 250 && mouseX < 350) {
    if (mouseIsPressed){
    background(0,255,0);
}
   fill(255,0,200); 
}
  rectMode(CENTER);
  rect(300,200,100,100); 



